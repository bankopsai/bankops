/** Global engine defaults shared by the layout engine, annotator and PPTX renderer. */
export const LINE_SPACING = 1.0; // line-height multiplier (1.0 = single spacing)

const Defaults = { LINE_SPACING };
export default Defaults;
